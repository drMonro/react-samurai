import styles from './Login.module.css';

export const Login = ({}) => {
    // if (!profile) {
    //     return <Preloader/>
    // }
    return (
        <section className={styles._}>
            <h1>Login</h1>
        </section>
    );

}

export default Login;
